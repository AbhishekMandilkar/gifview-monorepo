{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test:api": "node test/api-test.js"
  },
  "dependencies": {
    "@gifview-monorepo/auth": "workspace:*",
    "@gifview-monorepo/db": "workspace:*",
    "@gifview-monorepo/env": "workspace:*",
    "better-auth": "catalog:",
    "cors": "^2.8.5",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "express": "^5.1.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@gifview-monorepo/config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "typescript": "catalog:"
  }
}
